<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <!-- ================================================================== -->
    <!-- KANBAN VIEW (Odoo 19 Visual Pipeline Board)                        -->
    <!-- ================================================================== -->
    <record id="view_security_exception_kanban" model="ir.ui.view">
        <field name="name">security.exception.kanban</field>
        <field name="model">security.exception</field>
        <field name="arch" type="xml">
            <kanban default_group_by="state" class="o_kanban_small_column" sample="1">
                <field name="id"/>
                <field name="reference"/>
                <field name="name"/>
                <field name="owner_id"/>
                <field name="risk_level"/>
                <field name="state"/>
                <field name="expiration_date"/>
                <field name="days_until_expiry"/>
                <field name="is_expired"/>
                <field name="color"/>
                <templates>
                    <t t-name="card">
                        <div class="oe_kanban_card oe_kanban_global_click p-2">
                            <!-- Card Header: Reference & Risk Badge -->
                            <div class="d-flex justify-content-between align-items-center mb-1">
                                <span class="fw-bold text-muted" t-out="record.reference.value"/>
                                <span t-if="record.risk_level.raw_value == 'critical'" class="badge bg-danger">Critical Risk</span>
                                <span t-if="record.risk_level.raw_value == 'high'" class="badge bg-warning text-dark">High Risk</span>
                                <span t-if="record.risk_level.raw_value == 'medium'" class="badge bg-info text-dark">Medium Risk</span>
                                <span t-if="record.risk_level.raw_value == 'low'" class="badge bg-success">Low Risk</span>
                            </div>

                            <!-- Card Title -->
                            <div class="fw-bold fs-6 text-truncate mb-2" t-out="record.name.value"/>

                            <!-- Expired Warning Tag -->
                            <div t-if="record.is_expired.raw_value" class="badge bg-danger text-white mb-2">
                                ⚠ EXPIRED
                            </div>
                            <div t-elif="record.days_until_expiry.raw_value &gt; 0 and record.days_until_expiry.raw_value &lt;= 14" class="badge bg-warning text-dark mb-2">
                                ⏳ Expires in <t t-out="record.days_until_expiry.value"/> days
                            </div>

                            <!-- Card Footer: Owner Avatar & Expiration -->
                            <div class="d-flex justify-content-between align-items-center mt-2 pt-2 border-top">
                                <small class="text-muted">
                                    📅 Exp: <field name="expiration_date"/>
                                </small>
                                <field name="owner_id" widget="many2one_avatar_user" string="Owner"/>
                            </div>
                        </div>
                    </t>
                </templates>
            </kanban>
        </field>
    </record>

    <!-- ================================================================== -->
    <!-- FORM VIEW (Intuitive Single-Screen Inputs & Assignments)           -->
    <!-- ================================================================== -->
    <record id="view_security_exception_form" model="ir.ui.view">
        <field name="name">security.exception.form</field>
        <field name="model">security.exception</field>
        <field name="arch" type="xml">
            <form string="Security Exception">
                <header>
                    <!-- Clear Intuitive Workflow Buttons with Action Icons -->
                    <button name="action_submit"
                            string="▶ Submit for Assessment"
                            type="object"
                            class="oe_highlight"
                            invisible="state != 'draft'"/>
                    <button name="action_assess"
                            string="🔍 Complete Review Assignment"
                            type="object"
                            class="oe_highlight"
                            invisible="state != 'assessment'"/>
                    <button name="action_recommend_approval"
                            string="📋 Recommend Approval"
                            type="object"
                            class="oe_highlight"
                            invisible="state != 'review'"/>
                    <button name="action_reject"
                            string="❌ Reject Request"
                            type="object"
                            class="btn-secondary"
                            invisible="state not in ('review', 'pending_approval')"/>
                    <button name="action_approve"
                            string="👍 Approve Exception"
                            type="object"
                            class="oe_highlight"
                            invisible="state != 'pending_approval'"/>
                    <button name="action_initiate_review"
                            string="🔄 Initiate Periodic Review"
                            type="object"
                            class="oe_highlight"
                            invisible="state != 'active'"/>
                    <button name="action_renew"
                            string="Extend Renewal"
                            type="object"
                            class="oe_highlight"
                            invisible="state != 'under_review'"/>
                    <button name="action_request_verification"
                            string="Request Verification"
                            type="object"
                            class="btn-secondary"
                            invisible="state != 'under_review'"/>
                    <button name="action_verify_pass"
                            string="✅ Verify &amp; Pass (Close)"
                            type="object"
                            class="oe_highlight"
                            invisible="state != 'pending_verification'"/>
                    <button name="action_verify_fail"
                            string="❌ Verify &amp; Fail"
                            type="object"
                            class="btn-danger"
                            invisible="state != 'pending_verification'"/>
                    <button name="action_revise"
                            string="✏ Revise Draft"
                            type="object"
                            class="btn-secondary"
                            invisible="state != 'rejected'"/>

                    <!-- Status Bar -->
                    <field name="state"
                           widget="statusbar"
                           statusbar_visible="draft,assessment,review,pending_approval,active,closed"/>
                </header>

                <sheet>
                    <!-- Expired Banner -->
                    <div class="alert alert-danger mb-3" role="alert" invisible="not is_expired">
                        <strong>⚠ THIS EXCEPTION IS EXPIRED!</strong>
                        Please initiate a review to extend renewal or perform remediation verification.
                    </div>

                    <!-- Top Title & Reference -->
                    <div class="oe_title mb-3">
                        <span class="badge bg-primary fs-6 mb-1">Security Exception Record</span>
                        <h1>
                            <field name="name" placeholder="Enter Exception Title (e.g. Legacy TLS 1.0 Authentication Server)..."
                                   readonly="state in ('closed', 'rejected')"/>
                        </h1>
                        <h3>
                            <span class="text-muted">Reference ID: </span>
                            <field name="reference" readonly="1"/>
                        </h3>
                    </div>

                    <!-- Main 2-Column Section: Assignees & Timeline -->
                    <group>
                        <!-- Column 1: Assignees & Ownership -->
                        <group string="👤 Assignees &amp; Responsibilities">
                            <field name="requester_id" string="Requested By (Submitter)"/>
                            <field name="owner_id" string="Risk Owner (Accountable)"/>
                            <field name="reviewer_id" string="Security Reviewer"/>
                            <field name="approver_id" string="Approving Manager"/>
                        </group>

                        <!-- Column 2: Risk Severity & Timeline -->
                        <group string="⚡ Risk Ranking &amp; Deadlines">
                            <field name="risk_level" string="Risk Severity" widget="radio" options="{'horizontal': true}"/>
                            <field name="likelihood" string="Exploit Likelihood" widget="radio" options="{'horizontal': true}"/>
                            <separator string="Target Deadlines"/>
                            <field name="start_date" string="Effective Start Date"/>
                            <field name="review_date" string="Scheduled Review Date"/>
                            <field name="expiration_date" string="Expiration Deadline"/>
                            <field name="days_until_expiry" string="Days Remaining" readonly="1"/>
                            <field name="is_expired" invisible="1"/>
                            <field name="renewal_count" string="Renewal Count" invisible="renewal_count == 0"/>
                        </group>
                    </group>

                    <!-- Main Section: Justification & Description (Right in front of user) -->
                    <group string="📝 Details &amp; Justification">
                        <field name="business_justification"
                               string="Business Justification"
                               placeholder="Why is this security exception necessary? (e.g. Vendor delay, critical business dependence)..."/>
                        <field name="technical_justification"
                               string="Technical Justification"
                               placeholder="What technical obstacles prevent fixing this issue immediately?..."/>
                        <field name="description"
                               string="Full Summary"
                               placeholder="Detailed technical summary of the affected system, architecture, or vulnerability..."/>
                    </group>

                    <!-- Notebook Tabs for Controls & Verification -->
                    <notebook>
                        <!-- Tab 1: Compensating Controls -->
                        <page string="🛡 Compensating Risk Controls" name="controls">
                            <field name="control_ids">
                                <list editable="bottom">
                                    <field name="sequence" widget="handle" string=" "/>
                                    <field name="name" string="Control Name"/>
                                    <field name="control_type" string="Control Type"/>
                                    <field name="status" string="Status" widget="badge"/>
                                    <field name="responsible_id" string="Responsible Person"/>
                                    <field name="implementation_date" string="Date Implemented"/>
                                </list>
                            </field>
                        </page>

                        <!-- Tab 2: Detailed Impact Assessment -->
                        <page string="📊 Risk Impact Assessment" name="impact_assessment">
                            <group>
                                <field name="impact" string="Business Impact" placeholder="What is the potential impact if this risk is exploited?"/>
                                <field name="risk_description" string="Technical Risk Details" placeholder="CVSS scores, CVE IDs, or technical vectors..."/>
                            </group>
                        </page>

                        <!-- Tab 3: Remediation Verification -->
                        <page string="🔍 Remediation Verification" name="verification"
                              invisible="state not in ('pending_verification', 'closed')">
                            <group>
                                <group string="Verification Outcome">
                                    <field name="verification_result" string="Outcome"/>
                                    <field name="verified_by_id" string="Verified By"/>
                                    <field name="verification_date" string="Verification Date"/>
                                </group>
                                <group string="Audit Findings">
                                    <field name="verification_notes"
                                           string="Verification Findings"
                                           placeholder="Record vulnerability scan results or security audit evidence..."/>
                                </group>
                            </group>
                        </page>
                    </notebook>
                </sheet>

                <!-- Chatter Audit Log -->
                <div class="oe_chatter">
                    <field name="message_follower_ids"/>
                    <field name="activity_ids"/>
                    <field name="message_ids"/>
                </div>
            </form>
        </field>
    </record>

    <!-- ================================================================== -->
    <!-- LIST VIEW                                                           -->
    <!-- ================================================================== -->
    <record id="view_security_exception_list" model="ir.ui.view">
        <field name="name">security.exception.list</field>
        <field name="model">security.exception</field>
        <field name="arch" type="xml">
            <list string="Security Exceptions"
                  decoration-danger="risk_level == 'critical' or is_expired == True"
                  decoration-warning="risk_level == 'high'"
                  decoration-muted="state == 'closed'">
                <field name="reference" string="Reference ID"/>
                <field name="name" string="Exception Title"/>
                <field name="owner_id" string="Risk Owner"/>
                <field name="risk_level" string="Risk Severity" widget="badge"/>
                <field name="state" string="Status Stage" widget="badge"/>
                <field name="review_date" string="Scheduled Review Date"/>
                <field name="expiration_date" string="Expiration Deadline"/>
                <field name="days_until_expiry" string="Days Remaining"/>
                <field name="is_expired" column_invisible="1"/>
            </list>
        </field>
    </record>

    <!-- ================================================================== -->
    <!-- SEARCH VIEW                                                         -->
    <!-- ================================================================== -->
    <record id="view_security_exception_search" model="ir.ui.view">
        <field name="name">security.exception.search</field>
        <field name="model">security.exception</field>
        <field name="arch" type="xml">
            <search>
                <field name="name" string="Title"/>
                <field name="reference" string="Reference ID"/>
                <field name="owner_id" string="Risk Owner"/>
                <field name="reviewer_id" string="Security Reviewer"/>
                <field name="requester_id" string="Requested By"/>

                <!-- Filters -->
                <filter name="filter_my_exceptions"
                        string="My Exceptions"
                        domain="['|', ('requester_id', '=', uid), ('owner_id', '=', uid)]"/>
                <separator/>
                <filter name="filter_draft"
                        string="Draft"
                        domain="[('state', '=', 'draft')]"/>
                <filter name="filter_active"
                        string="Active"
                        domain="[('state', '=', 'active')]"/>
                <filter name="filter_pending_approval"
                        string="Pending Approval"
                        domain="[('state', '=', 'pending_approval')]"/>
                <filter name="filter_closed"
                        string="Closed"
                        domain="[('state', '=', 'closed')]"/>

                <!-- Group By -->
                <group>
                    <filter name="groupby_state"
                            string="Status Stage"
                            context="{'group_by': 'state'}"/>
                    <filter name="groupby_risk_level"
                            string="Risk Severity"
                            context="{'group_by': 'risk_level'}"/>
                    <filter name="groupby_owner"
                            string="Risk Owner"
                            context="{'group_by': 'owner_id'}"/>
                </group>
            </search>
        </field>
    </record>

    <!-- ================================================================== -->
    <!-- WINDOW ACTIONS                                                      -->
    <!-- ================================================================== -->

    <!-- All Exceptions -->
    <record id="action_security_exception" model="ir.actions.act_window">
        <field name="name">Security Exceptions</field>
        <field name="res_model">security.exception</field>
        <field name="view_mode">kanban,list,form</field>
        <field name="help" type="html">
            <p class="o_view_nocontent_smiling_face">
                Create your first security exception
            </p>
            <p>
                Track and manage temporary security exceptions through their
                complete lifecycle: creation, review, approval, monitoring,
                verification, and closure.
            </p>
        </field>
    </record>

    <!-- My Exceptions -->
    <record id="action_security_exception_my" model="ir.actions.act_window">
        <field name="name">My Exceptions</field>
        <field name="res_model">security.exception</field>
        <field name="view_mode">kanban,list,form</field>
        <field name="context">{'search_default_filter_my_exceptions': 1}</field>
    </record>

    <!-- To Review -->
    <record id="action_security_exception_to_review" model="ir.actions.act_window">
        <field name="name">To Review</field>
        <field name="res_model">security.exception</field>
        <field name="view_mode">kanban,list,form</field>
    </record>

</odoo>
